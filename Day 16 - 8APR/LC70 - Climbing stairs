class Solution {
    public int climbStairs(int n) {
        if(n<=2) return n;
        int cal=1;
        int a=0;
        int b=1;
        for(int i=0;i<n;i++){
            cal+=a;
            int next=a+b;
            a=b;
            b=next;
        }
        return cal;
    }
}
