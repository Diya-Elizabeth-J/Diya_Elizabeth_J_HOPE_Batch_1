class Solution {
    public int arraySign(int[] nums) {
        int signcount=0;
        for(int i : nums){
            if(i==0) return 0;
            if(i<0)
                signcount++;
        }
        
        if(signcount%2==0) return 1;
        else return -1;
    }
}
