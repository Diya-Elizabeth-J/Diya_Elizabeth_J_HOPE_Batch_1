class Solution {
    public int hammingDistance(int x, int y) {
        int op =x^y;
        int count=0;
        while(op>0){
            count+=op&1;
            op=op>>1;
        }
        return count;

    }
}
